# Demo mode — set to true when deploying publicly to Vercel
# When true, the API serves sanitized dummy data instead of real workspace files
NEXT_PUBLIC_DEMO_MODE=false

# Only needed for local dev with real file access (ignored in demo mode)
WORKSPACE_ROOT=C:\Users\ethan\.openclaw\workspace
